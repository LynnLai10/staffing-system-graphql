const Freetime = {
  // async myFreetimes(parent, args, { prisma, request }, info) {
  //   const { myFreetimes } = parent
  //   return myFreetimes.map((item, index) =>
  //   myFreetimes.find(
  //       (item) => item.schedule_day.day_No.split("_")[1] === index.toString()
  //     )
  //   );
  // }
}

export { Freetime as default }